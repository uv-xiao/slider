# References (gitignored)

Put per-style reference material here (images, PDFs, slide decks).

Suggested structure:

- `references/<style-name>/...`

Everything under `references/` is gitignored except this file.

