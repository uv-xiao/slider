# Prompts

- `content/`: per-page “content prompts” (v2; no style yet)
- `styled/`: per-page “styled prompts” (v2; ready for artifact generation)
- `generated/`: legacy per-slide prompts rendered from `specs/*.md` via the `slider` CLI
- `workflow-prompt.md`: legacy workflow prompt template used by `slider render-prompts`
